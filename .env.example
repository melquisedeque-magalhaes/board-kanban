DATABASE_URL="postgresql://kanban:kanban@localhost:5432/kanban?schema=public"
MCP_TOKEN="troque-este-token"

# Clerk (auth) — chaves no dashboard.clerk.com (ou rode: npx clerk@latest init)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_xxx"
CLERK_SECRET_KEY="sk_test_xxx"

# Pusher (push real-time, OPCIONAL) — sem isso, cai no polling do React Query.
# Crie um app free em dashboard.pusher.com → Channels → App Keys.
PUSHER_APP_ID=""
PUSHER_KEY=""
PUSHER_SECRET=""
PUSHER_CLUSTER=""
NEXT_PUBLIC_PUSHER_KEY=""
NEXT_PUBLIC_PUSHER_CLUSTER=""
